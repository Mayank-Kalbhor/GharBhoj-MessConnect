{
  "name": "messconnect-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "start:api": "npm run start:dev --workspace=apps/api"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
